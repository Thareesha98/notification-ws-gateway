spring:
  application:
    name: notification-ws-gateway   # must match the filename in config-repo
  config:
    import: optional:configserver:http://localhost:8888

# Local fallback if config-server unreachable
server:
  port: 8083
